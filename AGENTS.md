# AGENTS.md

本文件是 NovelRipple 唯一的仓库级工程规则，适用于在本仓库中分析、设计、修改、审查和验证工作的所有编码 Agent。除非未来某个目录出现无法在根规则中表达的真实差异，否则不要新增嵌套 `AGENTS.md`。

这里只保留长期稳定的产品不变量、工程约束和工作方法。技术栈、脚本和目录尚未确定时，不要猜测它们；确定后以仓库中的可执行配置为准。所有命令默认从仓库根目录运行。

## 权威文档索引

- 当前阶段、产品范围与已冻结基线：`docs/mvp.md`
- 领域语义与不变量：`docs/domain.md`
- 质量、评测规则与封版报告索引：`docs/evals.md`
- M1 Benchmark 分类、权利边界与 manifest：`benchmarks/m1/`
- 已采纳的技术决策：`docs/decisions/`

实现计划只引用这些事实源，不复制一套可能漂移的产品或领域定义。

## 项目使命

NovelRipple 将完成的小说转化为可追溯、可探索、可分叉的故事世界。首个产品闭环只有：

```text
导入作品 → 提炼故事地图 → 改变一个节点 → 预览连锁影响 → 创建并继续新世界线
```

任何架构、抽象或功能都必须能说明它如何让这条路径更可靠、更快或更容易使用。不能直接服务当前闭环的复杂度，默认不引入。

## 决策依据

出现冲突时，按以下顺序判断：

1. 当前用户要求和对应任务的验收标准；
2. 本文件中的产品不变量、安全边界和复杂度约束；
3. 当前代码、Schema、测试、构建脚本和 CI 的真实行为；
4. 当前 README、产品文档和已记录决策；
5. NarrativeOS、历史方案与外部研究材料。

现有实现不自动等于正确。任务范围内发现文档、测试和代码不一致时，先根据用户目标判断正确契约，再在同一变更中修正相关一方；任务外漂移只报告，不顺带扩张范围。

## 第一性原则

- **先定义用户结果**：动手前用一句话说明用户完成后能做什么，并写出最小验收场景。
- **最小完整切片**：优先交付可端到端运行和验证的纵向路径，不先铺设大而空的横向平台。
- **复杂度必须偿还成本**：新模块、服务、依赖、配置和抽象必须减少更多代码、认知或运维成本。
- **一个事实源，一个所有者**：同一规则只实现一次；Schema、领域判断和状态转换不得散落复制。
- **替换而非叠加**：契约改变时同步迁移调用方和测试，并删除废弃路径；不保留无用户需求的双轨、shim 或兼容包装。
- **AI 输出不是事实**：模型结果先作为候选，经过结构校验、证据检查和领域约束后才能写入正式状态。
- **数据安全高于便利**：无法确认目标、版权、分支或迁移影响时停止写入并明确报告，不猜测。
- **复用优于自建**：先找已验证的实现，再决定是否需要编写新代码。

## 每次迭代前的开源预检

任何改变产品行为、架构、数据模型、依赖或用户流程的迭代，编码前必须完成一次轻量开源预检。纯拼写修复不需要虚构候选项目，但必须明确说明它不是产品迭代。

### 预检步骤

1. 先写清问题、边界和必要接口，不拿某个库反向定义需求。
2. 检索官方文档、GitHub 官方仓库及维护者发布信息，通常比较 2–5 个真正相关的候选。
3. 至少检查：许可证、近期维护与发布、Issue 状态、社区采用、依赖体积、API 适配度，以及安全和隐私影响。
4. 按以下顺序选择：
   1. 复用 NovelRipple 已有能力；
   2. 从 NarrativeOS 提取一个边界清楚、已验证的小模块或模式；
   3. 直接采用成熟的开源依赖或协议；
   4. 在前三项都不合适时实现最小自有代码。
5. 在 Issue、PR 描述或本次交付说明中记录 `Open-source preflight`：检索日期、候选链接、许可证、采用或不采用的决定，以及为什么这是总复杂度最低的选择。

### 复用约束

- 只引用第一方仓库和官方文档作技术依据；博客可用于发现线索，不能替代源代码、许可和官方文档核查。
- 未确认许可证兼容性前不得复制代码。复制或改编时保留必要的版权声明、许可证和归属信息。
- “有现成库”不等于“必须加依赖”。只有它能显著减少自有代码和维护面时才引入。
- 不引入功能重叠的多个库。新增依赖必须说明用途、替代方案、锁定方式和移除成本。
- 研究要有时间边界；得到足以做出可逆决策的证据后就进入最小实现，不把调研本身做成长期工程。

## 与 NarrativeOS 的关系

NovelRipple 是独立产品，不继承 NarrativeOS 的整体架构。可以优先评估其中已经验证的原则，例如不可变修订、分支隔离、证据关联、候选先于提交和最小上下文；不得未经验证就复制它的仓库结构、运行时 Harness、服务层级、桌面装配、多 Agent 流程或兼容历史。

复用 NarrativeOS 代码时必须满足：

- 能独立提取，边界和许可证清楚；
- 不拖入无关包、服务或部署要求；
- 命名和领域含义适合 NovelRipple，而不是保留旧产品概念；
- 有针对当前用户路径的测试；
- 复用后的总代码和认知成本低于重写一个小实现。

## 复杂度预算

默认从一个可部署应用、一个主要数据存储和清晰的内部模块开始。故事具有图结构，不代表必须使用图数据库；先用最简单、可查询、可迁移的表示，只有真实数据与基准证明关系查询成为瓶颈时才升级。

没有当前验收需求和具体证据时，不引入：

- 微服务、分布式事务、事件总线或多套部署单元；
- 专用图数据库、向量数据库或重复的搜索系统；
- 通用工作流引擎、插件系统、自定义 DSL 或大型规则引擎；
- 多 Agent 编排框架、多模型路由层或预生成全部世界线；
- 为假想未来保留的 feature flag、adapter、facade 或兼容双轨。

耗时导入确实超出请求生命周期时，可以加入最简单的可恢复后台任务；先证明需要，再选择机制。新抽象应至少服务两个真实调用方，或明确隔离一个易变化的外部边界，并且删除的复杂度多于新增复杂度。

界面默认只突出一个首要操作，并逐步揭示高级信息。普通读者无需理解图数据库、向量、Prompt、Agent 或内部状态机，便能看懂故事地图并完成首次分叉。

## 最小领域语言

| 名称 | 含义 |
| --- | --- |
| **原著 Source** | 用户导入的原始文本及其定位信息；只读，不被生成内容覆盖 |
| **故事地图 Story Map** | 从原著提炼出的结构化解释，包含人物、事件、关系、线索、因果和世界规则 |
| **世界线 Worldline** | 一条完整叙事历史；原著基线与每个新分支都拥有独立身份 |
| **分歧点 Divergence** | 新世界线首次偏离其父世界线的节点 |
| **锚点 Anchor** | 用户要求在严格模式中保留的事实、状态或终局条件 |

新术语只有在消除真实歧义时才增加。不要用复杂术语掩盖尚未明确的产品行为。

## 产品不变量

- 原著文本和原著世界线不可被覆盖。修正提取结果应创建可追踪的新版本，不静默改写来源。
- 每个重要提取结论都要关联原文位置，并区分 `fact`、`inference` 和 `generated`；推断需带可解释的置信度或不确定性。
- 所有分支状态、检索、缓存和模型上下文都必须显式限定世界线。分歧前的不可变历史可以共享引用，分歧后的可变状态绝不串线。
- 严格模式只锁定用户选择的锚点，不要求中间过程复刻原著。当锚点与新因果无法同时成立时，展示冲突并允许调整，不用巧合强行收束。
- 完全开放模式下，原著在分歧点之后的事件不再是隐藏目标；但已经发生的历史、角色认知和未被用户修改的世界规则仍然有效。
- 先生成结构化的影响计划，列出人物、关系、线索、目标、风险和潜在终局变化；用户确认后再生成长篇正文。
- 模型输出必须经过 Schema、引用、权限和领域约束校验。失败或重试不得留下半提交状态；可重试操作应幂等。
- 派生索引、摘要和可视化应能从原著、正式故事地图与世界线事件重建。
- 不把用户作品正文、密钥、完整 Prompt 或私人分支写入日志、公开夹具、截图和错误报告。

## 工作流程

### 1. 理解

- 读取目标文件、直接调用方、相邻测试和当前任务说明；搜索优先使用 `rg`。
- 说明用户结果、验收场景、非目标和可能影响的数据。
- 检查工作区状态，保留并避开用户的无关改动。

### 2. 研究与设计

- 完成并记录开源预检。
- 选择最小可运行纵向切片，明确数据所有者、失败方式和回滚边界。
- 优先复用现有模块；需要新依赖或新抽象时给出净减复杂度的理由。

### 3. 实现

- 一次改动只解决一个清晰问题，同时更新相关调用方、Schema、测试和必要文档。
- 让 API、状态转换和模型结构显式、可验证；避免依赖隐式 Prompt 约定。
- 替换实现时删除旧路径、未使用导出和过时测试，不留下“以后再清理”的平行系统。
- 数据迁移默认保留当前契约下的有效用户数据；删除或不可逆转换必须得到当前任务明确授权。

### 4. 验证

- 运行仓库实际提供的格式化、静态检查、测试和构建脚本，不猜测命令，不伪造通过结果。
- 至少手工或自动验证受影响的完整用户路径，而不只验证内部函数。
- 报告实际执行的命令、结果、未执行项及原因。无法运行的验证不得用推断替代。

## 测试重点

测试用户可见行为和领域不变量，避免复制内部实现或堆积脆弱快照。与功能相关时优先覆盖：

- 提取结论能定位到正确原文，事实与推断不会混淆；
- 原著、父世界线和兄弟世界线之间完全隔离；
- 严格模式保留选定锚点，同时允许中间路径变化；
- 完全开放模式不会暗中把故事拉回原著未来；
- 导入、生成、提交和重试具备幂等性，不覆盖有效状态；
- 无效或缺证据的模型输出被拒绝，失败后仍可恢复；
- 最小端到端旅程可从导入走到创建新世界线。

测试夹具使用自建短篇或明确的公版文本，不提交真实用户作品。

## 仓库与安全纪律

- 修改真实文件使用小而清晰的补丁；不重写用户的无关改动。
- 禁止使用会清空工作区或大范围覆盖数据的破坏性命令，除非用户明确指定目标并授权。
- 不提交密钥、令牌、真实作品、生成缓存、大型模型产物或本地环境文件。
- 外部调用只发送任务必要的最少数据，并设置明确的超时、错误处理和可观察边界。
- 依赖变更同步更新锁文件，并核查许可证、维护状态与已知安全风险。
- Commit 和 PR 保持单一意图；不要把顺手重构、格式化全仓库或无关升级混入当前任务。

## Definition of Done

一项变更只有同时满足以下条件才算完成：

- 当前用户结果和验收场景可以实际完成；
- 原著不可变、证据可追溯、分支隔离和所选模式语义没有被破坏；
- 开源预检已有简短记录，复用与自建决定可解释；
- 适用的测试、静态检查和构建已通过，或未执行项已明确说明；
- 没有新增无用依赖、平行实现、死代码、秘密或用户正文泄露；
- 相关文档描述当前真实行为，不承诺尚未实现的功能。

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
