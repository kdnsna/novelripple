# M0 — First Ripple 正式封版报告（PASS）

> 本报告记录 `v0.1.0` 的确定性发布门禁 PASS。它不宣称真实模型 Live Eval 已通过；模型质量评测与默认 CI 明确分离。

## 封版身份

- Tag：`v0.1.0`
- Annotated tag object：`b48411207e31d332b84794c233697a7753a193ce`
- Tag 指向提交：`b627cab54fb84d742cb72cc49b0c2ba9bbe1f528`
- Release：[`M0 — First Ripple`](https://github.com/kdnsna/novelripple/releases/tag/v0.1.0)，发布于 `2026-08-12T02:28:04Z`，非 draft、非 prerelease
- 封版 CI：[`M0 gate / 31550268197`](https://github.com/kdnsna/novelripple/actions/runs/31550268197)，head SHA 为 `b627cab54fb84d742cb72cc49b0c2ba9bbe1f528`

本报告是在 M0 tag 之后补充的历史索引，不移动、重打或修改 `v0.1.0`。合并提交 `8ab301f520c45e1c0c316ff6742f510158820711` 将 tag 所在提交合入 `main`，其工作树内容与 tag 一致。

## 确定性发布门禁

GitHub Actions 的 `Node 22 + Chromium` job 于 `2026-08-12T00:29:48Z` 完成，结论为 `success`。以下封版步骤全部通过：

- `npm run lint`
- `npm run typecheck`
- `npm run test:unit`（138 passed）
- `npm run test:contract`（4 passed）
- `npm test`（142 passed）
- `npm run build`
- `CI=1 npm run test:e2e`（7 passed）

因此，`v0.1.0` 的正式结论是：**M0 确定性发布门禁 PASS**。

## Live Eval 边界

仓库中 `2e85d21c5766a1dd4595cc26dbb181d99287b58b` 的 Live Eval 尝试因没有真实 provider、model 和 Structured Output 配置，在调用模型前 fail closed。该 [`Historical / FAIL 记录`](2026-08-12-2e85d21-m0-live-eval.md) 没有生成模型输出，不能被解释为真实模型质量 PASS，也不应删除或覆盖。

M0 Release 封版的是已在默认 CI 中执行的确定性工程基线，以及真实模型 Eval 的可运行、脱敏、fail-closed 验收机制。真实模型质量仍须按 `docs/evals.md` 独立运行和人工复核。

## M1 启动校准

- 校准日期：`2026-08-12`
- 校准起点的远端 `main`：`8ab301f520c45e1c0c316ff6742f510158820711`
- GitHub milestone：`M1 — Real Story`，状态为 open，具体 Issues 为 0
- 仓库 open issues：0
- M0 冻结规则：除 P0/P1 bug 外不修改 M0 代码；M1 基于现有 M0 能力演进

本次只校准阶段文档、封版索引和历史报告标签，不实现 Story Map、Ripple、Continuation 或任何 M1 产品功能。

## Open-source preflight

- 检索日期：`2026-08-12`
- 问题与边界：修正文档阶段状态漂移，建立现有 M0 Release / CI 事实的唯一索引；不改变产品行为、架构、数据模型、依赖、Prompt 或用户流程
- 候选：复用 NovelRipple 当前 README、权威文档、Schema、测试、CI 与 GitHub Release / milestone；从 NarrativeOS 提取；引入外部工具或依赖
- 决定：只复用 NovelRipple 现有事实源。后两者不解决文档状态问题，反而增加平行定义和维护面，因此不采用
- 许可证与安全：未复制外部代码或资产，未新增依赖，未读取或记录密钥、真实作品正文、完整 Prompt 或 raw model output

这是总复杂度最低的选择，也不建立第二套 Artifact、Eval 或 Provider 系统。
