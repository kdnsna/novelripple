# M1 Public Benchmarks

本目录只接收原创、公版或明确许可公开再分发的真实作品。每篇作品使用独立子目录，包含一个 UTF-8 `.txt` / `.md` Source、一个通过 `../manifest.schema.json` 校验的 `manifest.json`，以及权利要求所需的 attribution / license 文件。

M1-01 不提交任何作品正文。后续加入作品前必须先确认权利、字符数、Story class 和是否满足真正未见作品条件；不确定或不可公开的作品放入被 Git 忽略的 `benchmarks/private/`。
